<div class="container coasters-index">

  <a href="/parks/new" class="btn btn-dark">Crear nuevo parque</a>

  <h1 style="font-weight: 100">Índice de parques</h1>

  {{#each parks}}
  <hr>
  <a href="/parks/{{_id}}" class="btn btn-sm btn-dark">Ver detalles</a>
  <form action="/parks/delete/{{_id}}" method="post">  
    <button type="submit" class="btn btn-dark">Delete</button>
  </form>
  <h4>{{name}}</h4>
  <p>{{description}}</p>
  <hr>
  {{/each}}
 


</div>